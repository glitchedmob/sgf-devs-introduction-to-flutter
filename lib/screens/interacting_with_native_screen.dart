import 'package:flutter/material.dart';

class InteractingWithNativeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Interacting With Native'),
      ),
      body: Container(),
    );
  }
}
